/* Firebase project settings for the cloud library.
   Paste the `firebaseConfig` object from the Firebase console (Project settings → Your apps → Web)
   in place of null. While this is null the site runs exactly as before: libraries stay on the device
   and no sign-in button is shown. These values are public by design; access is governed by
   firestore.rules and storage.rules, not by hiding them. */
window.FIREBASE_CONFIG = null;
